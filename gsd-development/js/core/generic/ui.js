(function (ui, app, api, $, _, Backbone, undefined) {

    'use strict';

    var ajax;

    ui.page = {
        window: {
            el: $(window)
        }
    };

}(GSD.Ui = GSD.Ui || {}, GSD.App, GSD.Api, jQuery, _, Backbone));
